# Phase 1 结果、瓶颈诊断与下一步(2026-08-12)

`TRAINING_PLAN.md` 是方案文档,已经很长;这份只讲**Phase 1 实际跑出来的结果、瓶颈在哪、明天做什么**。
方案背景看 `TRAINING_PLAN.md`,代码踩坑看 `ecallisto_grabber/开发日志.md`。

---

## 一句话结论

管线完全跑通,Type III **检出率 95%、时间中位偏差 10.7 秒** —— 但 **AP50 只有 0.235**,
因为**catalog 标注的天花板就是 0.485**。而人工标注的天花板实测约 **0.88**。

**核心结论一(已用可证伪的实验确认):标注质量 > 标注数量。**
205 个人工精确标注的框(AP50 **0.269**)打赢 1500 个混合框(**0.184**),训练数据少 7.3 倍。
往训练集里混未审核数据在当前量级下是**负收益**,不是无益。

**核心结论二(已修正,原说法夸大):单类比三分类好约 19%,不是 4 倍。**
"4 倍"是拿 v5_single(570 训练框)去比 v3(243 框)得出的,**训练量不同,对比不公平**。
同为 570 框的公平对比:single AP@0.50 **0.361** vs multi **0.304**(+19%),
而 multi 的检出率(92% vs 88%)和时间偏差(6.2s vs 7.8s)反而更好。
让 v3 崩掉的是**训练框太少 + 三头分摊**,不是"分类任务本身有害"。

**核心结论三(最重要,且推翻了之前多处判断):Monstein 目录漏记约 36% 的真实事件。**
人工核查 34 个"虚警",**94% 其实是真 burst**。我们一直在用一把有缺口的尺子量模型。

---

## 跑了什么

四组,**val 完全相同**(18 station-day / 150 干净正样本窗口 / 169 框,已 `cmp` 逐字节验证):

| | train 正样本框 | 说明 |
|---|---|---|
| A noisy | 1344 | 只用未审核事件,catalog 时间 + 去偏 |
| B clean | 184 | 只用人工审为 usable 的 |
| C both | 1528 | 两者合并 |
| D wide | 1528 | 同 C,只把图从 640×640 换成 640×1280 |

统一配置:`yolov8s` COCO 预训练,冻结 backbone 5 epoch + 整体微调 25 epoch,`patience 8` 早停,
几何增广全关(频谱图翻转会破坏漂移方向这个核心判据)。

## 结果(Type III,151 个 val 框,唯一统计上可信的类)

| run | AP@0.30 | AP@0.50 | AP@0.75 | **检出率** | **\|Δt\| 中位** | \|Δt\| p80 | 虚警/小时 |
|---|---|---|---|---|---|---|---|
| A noisy | 0.476 | 0.235 | 0.018 | **95%** | 10.7s | 25.3s | 2.27 |
| B clean | 0.292 | 0.223 | 0.035 | 72% | 9.1s | 29.3s | **0.80** |
| C both | 0.469 | 0.203 | 0.024 | 88% | 11.0s | 25.4s | 0.84 |
| D wide | 0.303 | 0.204 | 0.024 | 89% | 9.5s | 40.4s | 1.14 |

**AP 和实用指标讲的是完全不同的故事**:AP50 = 0.235 看着很差,但同一个模型 151 个 Type III 里
找到了 **95%**,时间中位偏差只有 **10.7 秒**。原因见下面的"IoU 悬崖"。

**Type II 唯一的非零结果出现在 B clean**(AP@0.30 = 0.197、检出率 42%),其余三组全是 0。
val 里 II 只有 12 个框,不能当结论,但两套口径互相印证,方向上支持"人工修正的标注质量显著更高"。
**Type V 六个框全部漏检**,四组无一例外。

## 为什么 AP 这么低:IoU 悬崖

框是满频率高度(`h=1.0`),所以 **IoU 完全由时间轴决定**。两个等宽 `w` 的区间偏移 `d` 时
`IoU=(w−d)/(w+d)`,于是 **IoU≥0.5 要求 `d ≤ w/3`**:

| val 框宽度 | 容错窗口 |
|---|---|
| 30s(25% 分位) | **±10s** |
| 69s(中位) | ±23s |
| 180s(75% 分位) | ±60s |

而 catalog 时间的实测中位偏差是 **+40 秒**。模型的实际偏差是 10.7 秒 —— **恰好卡在阈值线上**,
所以一半预测掉到阈值下方,AP50 塌陷,而"找没找到"几乎不受影响。

### 标注天花板(关键数字)

假设模型**完美复现**训练标注约定(去偏后的 catalog),拿去和 val 的人工真值比:

| | 上限 |
|---|---|
| IoU≥0.50 命中率 | **48.5%** ← AP50 天花板 |
| IoU≥0.75 | 12.4% |
| **IoU≥0.95** | **0.0%** |
| 中位 IoU | 0.474 |

按标注来源拆开:val 里 44 个 catalog 来源的框只有 **29.5%** 达标,125 个人工修正的框有 **55.2%**。

**这解释了 AP50-95 为什么四轮实验纹丝不动(0.034–0.046)**:IoU≥0.95 的命中率是 0.0%,
那个指标从一开始就被标注锁死,和模型、和时间分辨率都无关。

---

## 五个瓶颈(按严重程度)

### 1. 标注时间不准 —— 最致命
catalog 中位偏差 40 秒,容错只有 10–23 秒。天花板 0.485。
**全人工标注能消掉这一项**(train/val 约定一致),但要标的是**训练集**——Arecibo 还有约 1450 条
正样本没修正过。剩下一个新天花板:人工标注自身的重复性,**已实测 ≈88%**(见下面"P0 两项已完成"),
足够高,所以扩大人工标注是有效投入。

### 2. 验证集太小 —— 分数不可信
169 个框(III 151 / II 12 / V 6)。两份独立的不确定性:一个本该中性的评测开关 `rect` 能让 AP50
从 0.176 变到 0.203(0.027);同配置换种子的极差是 0.020。**合起来约 0.03,而四组的极差是 0.051**
—— 所以只有 B vs C 那一档差异大到值得当真,A/D 之间读不出意义(详见下文)。
噪声大致按 `1/√n` 下降,**要把噪声减半,val 框数需翻 4 倍**(169 → 约 680)。

### 3. Type II / V 样本本来就少 —— 不是审核能解决的
**Arecibo 全部历史只有 53 个 II、27 个 V**(总正样本 1694,III 占 1614)。全审完也就这么多。
出路只有:换/加站点(七站合计 II 154、V 89,Australia-ASSA 独有 55 II + 32 V),或承认
Phase 1 只做 Type III。**这是数据源问题,不是标注问题,别混为一谈。**

### 4. 目录会漏记 burst —— 虚警率是上限
模型正确检出目录里没有的真实 burst 会被算成虚警。所以 2.27/h 是**上限**,真实值未知。
**只影响 precision/虚警,不影响检出率**,所以没有 1、2 急。
**测量方法**:把标为虚警的框抽 30–50 个丢进 `event_review` 人工看,直接把未知变已知。

### 5. 模型本身 —— 排最后
天花板 0.485、实测 0.235,**只发挥了 48%**。有空间,但天花板不抬,做到 100% 也只有 0.485。

---

## 本轮的三处更正(避免重走)

### ① "给框加频率范围能放宽 IoU" —— **错的,收益恒为零**
只要真值和预测用同一个频率范围(用"常用范围"必然如此),**频率项在二维 IoU 的分子分母里直接约掉**,
IoU 恒等于一维时间 IoU。数值验证:满高度、20-70MHz、30-50MHz、45-46MHz 四种设定下,同一个偏移
40 秒的预测 IoU 全部等于 0.2000。若真值和预测的频率范围不一致,反而更差(0.143)。

### ② "一维区间 IoU 比二维框更严苛,所以该放宽阈值" —— **错的**
若频率方向预测准确,二维 IoU **等于**一维;若不准,二维**更低**。我们的满高度口径其实比论文的
二维框**更宽松**。**天花板低完全是标注误差造成的,不是任务结构造成的**,所以换 IoU=0.3 的诚实定位是
"用与标注精度相称的阈值报告",**不能说成"和论文一样好"**(文献 III 0.803 用的是 mAP@50)。

### ③ "C 合并方案赢了(+22%)" —— **收回**
那是评测时 `rect` 开关不一致造成的。锁死口径后排名翻转(rect=False 下 **B 最好**)。
**注意:后来实测种子噪声(极差 0.020)小于四组极差(0.051),所以"全是噪声"这个说法我也说过头了**
—— 准确的说法是 A/D 相对 C 读不出意义,而 B 相对 C 的优势值得当真。见下文。

### 另外两条已证伪的路
- **自动时间修正**:能量峰值 + 阈值展开、catalog 约束搜索 + 固定宽度,共 10 种设定,在 258 条人工
  修正上全部**大幅落后**于什么都不做的去偏基线(IoU≥0.5:9–32% vs **56%**)。和文档早记过的
  "自动化能量指标不可靠"是同一失效模式。
- **把 `known_burst_weight` 传进 `robust_smooth_background`**:见开发日志模块四,背景会整个塌成 0。

---

## P0 两项已完成(2026-08-12 深夜)

### ① 噪声底:同配置换 3 个随机种子

配置 C(both / 640),只改 `--seed`:

| seed | III AP50 | AP50-95 | P | R |
|---|---|---|---|---|
| 0 | 0.203 | 0.058 | 0.385 | 0.318 |
| 1 | 0.183 | 0.040 | 0.272 | 0.344 |
| 2 | 0.185 | 0.043 | 0.309 | 0.338 |

**均值 0.190,标准差 0.011,极差 0.020。**(seed 0 = 0.203 与 `abc_both` 完全一致,管线可复现。)

**这推翻了我之前"四组差异全落在噪声内"的说法**——那个判断是基于 `rect` 开关造成的 0.027 差异,
但种子噪声(极差 0.020)其实**小于**四组的极差(0.051)。按 σ=0.011 算:

| | 差值 | σ 倍数 |
|---|---|---|
| A noisy vs C | +0.024 | 2.2σ |
| **B clean vs C** | **+0.051** | **4.6σ** |
| D wide vs C | +0.024 | 2.2σ |

**但不要据此宣布显著**,有两个必须一起说的限制:
1. **n=3 估出来的标准差本身极不确定**,95% 区间是 [0.006, 0.069]。若真实 σ 取上界,B vs C 只有 0.7σ。
2. **评测口径(`rect` 开关)本身就能造成 0.027 的差异**,与种子噪声同量级,是一份独立的不确定性。

**能说的最强结论**:A/D 相对 C 的差异(2.2σ,且小于评测口径本身的抖动)读不出意义;
**只有 B(只用人工审核数据)相对 C 的优势大到值得当真**——而且它和"B 是唯一 Type II 非零的一组"
互相印证。方向一致:**标注质量比标注数量重要**。184 个人工修正的框打赢了 1528 个混合框。

### ② 人工标注重复性 = 88%(但有水分,见下)

从已修正的 258 条随机抽 30 条,清空记录后**盲测重标**(工具预填 catalog 值,看不到旧答案),完成 25 条:

| | |
|---|---|
| 中位 IoU | **1.000** |
| **IoU≥0.50** | **88%** |
| IoU≥0.75 | 68% |
| 起点偏差 \|Δ\| | 中位 0.0s,p80 10.0s,最大 40s |
| 系统性漂移 | **+0.0s(无)** |

验真:**0 条**直接接受了 catalog 预填值,说明每条都真的动手判断过,盲测是干净的。
而且抽样偏窄(宽度中位 45s vs 全体 69s),按 `d≤w/3` 容错更小,**所以 88% 是保守估计**。

**天花板由此从 0.485(catalog 标注)抬到 ~0.88(人工标注)** ——
这把"继续人工标注"从一个有风险的赌注变成了**确定有效、且是当前最大的可用增量**。

**水分在哪(必须一起记住)**:25 对里有 **11 对(44%)是两次都没动起点**,它们的"一致"来自两次都没动手,
不是两次判断相同。所以 0.88 偏乐观;真实判断重复性更低,但样本(14 对)太小,不给具体数字。

## 由此发现的真实缺陷:38% 的人工修正只改了终点

追查"两次都没动起点"时发现的,**这是比重复性更要紧的问题**:

| | 数量 |
|---|---|
| 起点保持 catalog 预填值(**没动起点**) | **107 / 281(38%)** |
| 终点保持预填值 | 20 / 281(7%) |

动过起点的 174 条里,**96% 是往后挪,中位 +30s**——你自己的标注已经证明 catalog 起点系统性偏早。
最硬的证据是框宽:

```
没动起点的框   中位  90s
动过起点的框   中位  30s        <- 差 3 倍
```

那 107 条不是"burst 真的长",是左边多含了 catalog 偏早的一段。

**根因是操作成本不对称,不是判断能力问题**:catalog 的终点明显错(36% 是零宽度,一眼就不合理)→
93% 会动手改;catalog 的起点是个"看着正常的数字"→ 38% 被默认接受。

### 两项处置(已做)
1. **拖动框选**(`event_review/range_select.py`):在频谱图上横向拖一段同时设定起止,
   吸附到 1 秒。**一次手势设定两端,"只改一半"在物理上不再可能**——这是对症的修法。
   (踩坑:`go.Image` 会被 plotly.js 在**渲染时**锁成方形像素,Python 侧读 `scaleanchor` 是 `None`
   看不出来,图会被压成窄条;要显式传 `scaleanchor=False` 解除。)
2. **`data/ecallisto/fix_start/`**:107 条软链接的重审目录,保留原记录以便预填(终点已对,主要拖起点)。
   合并用 `event_review/merge_subset_reviews.py`,**默认 dry-run**,写入前自动备份,只更新这 107 条。

### 关于标注粒度(已验证,不用再纠结)
换成拖动后新标注是任意整数,旧的以 5/10 为主,**混用没有问题**:取整是**无偏**的(与 catalog 那种
+40s 的**系统性偏移**性质完全不同),10s 网格只给标注增加 6.6% 噪声、1s 网格增加 0.1%,
而人自身抖动是 σ≈7.8s。模拟显示混用两种网格相比同网格只差 1-3 个百分点。
**已否决的变体**:"起点向前取、终点向后取"的方向性扩展会让框系统性变宽 10%,
等于在人工标注内部再造一次约定分裂,和 0.485 那次同一种病。

## 修完 107 条后的 B/C 重跑 —— 预测被证实

修完那 107 条"只改终点没改起点"的记录后,刷新 boxes 并重建 B/C 重跑
(实验快照 `event_review/backups/snapshot_for_BC_rerun_20260812T200408.csv`:560 条审核 / 282 条时间修正;
新 val 185 框,两组逐字节相同):

| run | train 框 | III AP50 | III 50-95 | III P | III R | II AP50 |
|---|---|---|---|---|---|---|
| **B clean (v2)** | **205** | **0.269** | **0.108** | 0.326 | 0.302 | **0.114** |
| C both (v2) | 1500 | 0.184 | 0.048 | 0.156 | 0.462 | 0.032 |

```
B-C 差距:  修正前 +0.051  ->  修正后 +0.085   (拉大 +0.034)
```

**这是事先写下的可证伪预测,结果证实了它。** 合计噪声(种子 0.020 + 评测口径 0.027)约 0.03,
现在的差距是它的 **2.8 倍**,是目前最扎实的结论。

三个互相独立的佐证:
1. **AP50-95 第一次真正动了:0.108**(此前所有实验钉在 0.034-0.058)。这个指标衡量框贴合得准不准,
   修正起点后翻倍——"标注更准 → 定位更准"的直接证据。
2. **Type II 从 0.001 涨到 0.114**。
3. **205 个框打赢 1500 个**,训练数据少 7.3 倍,AP50 高 46%。

**C 组的失败模式也说明了问题**:precision 只有 0.156(B 是 0.326)而 recall 更高(0.462 vs 0.302)
——被 catalog 那套偏早约定带跑偏,**报得多但报不准**。

### 由此更新的行动准则
- **不要再往训练集里混未审核数据**(当前量级下是负收益)
- **每条人工修正的边际价值极高**:205 个精修框 = 0.269,1500 个混合框 = 0.184
- Arecibo 还有约 **1450 条正样本**未修正。按这个斜率,全部修完有希望接近文献水平

## v5:单类 vs 三分类,以及"目录不完整"这个更底层的问题

数据规模(快照 `snapshot_v5_20260813T010737.csv`,1007 条审核 / 648 条时间修正):
新的类别感知划分让 val 只取够用的量(II 21 / III 183 / V 20,共 224 框),
**训练框从 237 涨到 570**——按旧的 45% 比例规则,val 会白白吃掉 300 多个精修框。

### 单类是决定性的胜利

| | v3(243框·三类) | **v5_single(570框·单类)** |
|---|---|---|
| AP@0.30 | 0.230 | **0.459**(2.0×) |
| **AP@0.50** | 0.091 | **0.361**(**4.0×**) |
| **AP@0.75** | 0.020 | **0.177**(**8.9×**) |
| 检出率 | 47% | **88%** |
| \|Δt\| 中位 | 13.4s | **7.8s** |

训练稳定性同样翻转:最后 10 个 epoch 的 recall 极差 **0.040**(v3 是 **0.58**,相邻 epoch 的 mAP 能差 20 倍)。
**证实了推断:243 个框摊到三个检测头上,每个头样本太少导致训练崩溃**;合成一个目标后立刻收敛。

### 预测框的去向:不是乱报,是定位不准

818 个预测框(真值 224 个)的分布:

| | 数量 | 占比 |
|---|---|---|
| IoU≥0.5(算命中) | 318 | 38.9% |
| 0.3≤IoU<0.5 | 127 | 15.5% |
| 0<IoU<0.3 | 242 | 29.6% |
| IoU==0(完全在别处) | 131 | 16.0% |

**84% 的预测都压在真实 burst 上**,只是重合度不够。每个真值 burst 平均被 **3.2 个**预测框碰到
(75% 被 ≥2 个碰到)。所以精确率低的主因是**同一个 burst 被切成多个偏移不同的框**,不是虚报。

这也解释了为什么 NMS 调不动它:**NMS 只合并互相重叠超阈值的框**,而这些碎片彼此错开、各自蹭到真值
一角,NMS 认为它们是不同目标。置信度/NMS 网格扫描(NMS 0.1-0.7 × conf 0.05-0.40)最好只到
AP50 **0.390**(默认 0.361),提升 0.029,**证伪了"重复框"假设**。

### 后处理:选最高分优于合并

| 策略 | 框数 | 召回@0.5 | 精确@0.5 | 中位 IoU | **F1** |
|---|---|---|---|---|---|
| 原始 | 818 | 72% | 20% | 0.713 | 0.311 |
| union(并集) | 232 | 44% | 42% | 0.403 | 0.430 |
| **maxconf(选最高分)** | 232 | 53% | **51%** | 0.523 | **0.522** |
| wbf(置信度加权平均) | 232 | 46% | 45% | 0.463 | 0.456 |

顺序本身有信息:**并集和加权平均都把"偏掉的框"混进答案,maxconf 直接丢掉它们**。
一簇里"总有一个对得准",其余是噪声,不该参与平均。
注意原始那个 72% 召回是"一簇三个框只要有一个对上就算",**maxconf 的 53% 才是诚实的单次答案**。

## ⚠️ 最重要的发现:Monstein 目录漏记约 36% 的真实事件

把 131 个"完全不与任何真值重叠"的预测按置信度分层抽 34 个,人工逐个核查:

| 置信度 | n | 确认确实有 burst |
|---|---|---|
| 0.05-0.1 | 10 | 8(80%) |
| 0.1-0.2 | 10 | **10(100%)** |
| 0.2-0.35 | 10 | **10(100%)** |
| >0.35 | 4 | **4(100%)** |
| **总体** | **34** | **32 = 94%**(95% 区间 86-100%) |

**置信度 ≥0.1 的 24 个全部是真 burst,无一例外。**

### 三个数字要改

| | 报告值 | 修正后 |
|---|---|---|
| 虚警率 | 0.90/h | **约 0.05/h**(150 小时里真误报约 8 个) |
| Precision@0.5 | 0.20 | 严重低估——123 个正确检出被当成了错误 |
| 目录完整性 | 假定 100% | **漏记约 36%**(val 目录记 224 个,模型另找出约 123 个) |

### 这推翻了什么

1. **"AP 低是因为标注不准"不完整** —— 更底层的问题是**目录本身不完整**,
   每个"正确但未被目录记录"的检出都被计为 false positive,把 PR 曲线整体压低。
2. **训练集受到主动的错误监督** —— 那些含真实 burst 却被当作负样本(空标注)的窗口,
   在教模型"这里没有 burst"。**这比标注噪声更糟,是反向信号。**
3. **"模型报太多"是错的** —— 模型的置信度是可靠信号,看起来 precision 低完全是尺子的问题。

### 一个直接可用的推论
模型在置信度 ≥0.1 上准确率接近 100%,**可以用它反过来找目录漏记的事件,人工只需确认**。
这比从头审快得多,而且是当前扩充干净标注最高效的路径。

## v5_multi:分类那一半的如实汇报

| 类别 | val 框 | AP@0.30 | AP@0.50 | 检出率 | \|Δt\| 中位 |
|---|---|---|---|---|---|
| **III** | 183 | 0.406 | **0.296** | **91%** | **4.8s** |
| II | 21 | 0.201 | 0.173 | 62% | 118.2s |
| **V** | 20 | **0.048** | **0.048** | **15%** | 12.4s |

**Type III 已经可用**;II 有信号但定位很差(偏 118 秒);**V 基本失败**——只有 9 个训练框,
符合"27 个干净样本无法同时支撑训练和评估"的预判。

### single vs multi(同为 570 训练框,val 相同)

| | AP@0.30 | AP@0.50 | 检出率 | \|Δt\| 中位 |
|---|---|---|---|---|
| v5_single | 0.459 | **0.361** | 88% | 7.8s |
| v5_multi(类别无关口径) | 0.421 | 0.304 | **92%** | **6.2s** |

**结论修正**:single 在 AP 上领先约 19%,但检出率和定位精度略输。
**570 框规模下不需要为了识别定位而放弃分类** —— multi 同时给出不输 single 的检出定位,
外加一个可用的 Type III 分类器。建议主线走 multi,**把 V 如实标注为"样本不足,不做结论"**。

## 负样本污染:实测 4.2%,已剔除 90 个

模型在 val 的 382 个负样本窗口上有检出的比例:conf≥0.05 6.5% / **conf≥0.10 4.2%** /
conf≥0.20 2.1%。人工核查那 16 个(conf≥0.10)**14/16(88%)确实含 burst**。

**注意 train 只查出 1.9% 而 val 是 4.2%,这不是真实差异而是循环偏差** ——
模型在 train 负样本上训练过、被教过"这里没有 burst",所以在它们上面开火概率被系统性压低。
**val 的 4.2% 才是无偏估计**,推论是 train 里真实污染约 166 个而只标出 74 个,
**漏掉的近百个正是错误监督最强的那些**。

已建 `data/ecallisto/windows/contaminated_negatives.csv`(90 个)并由
`build_yolo_dataset.py --drop-contaminated-negatives` 剔除,`yolo6_single` 是剔除后的数据集,
**但还没训练过** —— 效果未知,见 `OVERNIGHT_PLAN.md`。

**这个剔除不是无损的**:人工核查显示筛法 88% 精确,即约 12% 会被误剔;而且置信度切不干净
(0.106 的检出是真的,0.114 和 0.167 的不是),所以没有阈值能修好这一点。

## 明天的计划(按优先级)

### ~~P0 测噪声底~~ / ~~P0 测标注重复性~~ —— **两项已完成,结果见上一节**

### ~~P0 重审 107 条~~ / ~~P0 重跑 B/C~~ —— **已完成,见上一节**
107 条全部修正(起点 88% 往后挪、框 93% 变窄,方向符合预期),写入前后校验通过
(559 条不变、变动行数恰好 107、起点未修正的从 107 降到 0)。B/C 重跑证实了预测。

### P0 查负样本的污染程度 —— 目录漏记 36% 的直接后果
836 个 `type=0` 窗口**一条都没审过**。按构造它们是"目录里没有事件的时段",但目录漏记 36%,
所以其中相当一部分很可能含真实 burst,却以**空标注**参与训练——这是在主动教模型
"这里没有 burst",比标注噪声更糟。先抽样量化污染率,再决定怎么处理。

### P0 用模型辅助扩充标注(而不是继续从头审)
模型在置信度 ≥0.1 上对"这里有 burst"的判断准确率 24/24。用它扫全部窗口、把高置信度
但目录没有的检出列出来,人工只做确认——比从头审快得多,而且直接补上目录的 36% 缺口。

### P1 把 maxconf 后处理写进 evaluate.py
F1 从 0.311 提到 0.522。这是能部署的口径,以后所有指标都该用它报。

### P1 继续人工时间修正(同时缓解瓶颈 1 和 2)
优先 Arecibo。目标量级:val 框数翻 4 倍才能把指标噪声减半。

### P1 测目录漏记率
抽 30–50 个"虚警"框人工审,把虚警率的上限变成实测值。

### P2 模型侧(天花板抬起来之后再做,按性价比)
1. **换成一维时间轴分割** —— 最贴合任务。框在 640 图上是 21–49 像素宽 × 640 高(长宽比 1:13~1:30),
   目标检测器不是为这种形状设计的;一维分割直接输出"时间区间 + 类型",正是我们要的东西。
2. **多通道输入** —— 现在只用了 PNG 的一个通道。可以塞进去去噪版、不同归一化尺度,几乎零成本。
3. **调超参** —— 学习率/epoch/冻结策略/置信度阈值目前全是随手定的,一个都没搜过。
4. ~~换更大模型~~ —— 不建议,1500 个训练框下 `yolov8s` 已经过拟合。

---

## 当前资产

- 审核:**515 条**(usable 337 · discard 176 · 待重试 2),其中 **258 条有人工时间修正**,全部 Arecibo
- 数据:`data/ecallisto/windows/` 6609 个 15 分钟窗口(4.5 GB,432 station-day)
- 数据集:`data/ecallisto/yolo_{noisy,clean,both,wide}`
- 权重:`detection/runs/abc_{noisy,clean,both,wide}/weights/best.pt`

## 相关代码

| | |
|---|---|
| `detection/refresh_boxes.py` | 从现有窗口 + 当前审核记录重新生成 `boxes.csv`,**不重新下载**。每审一批就跑一次,否则新审核进不了数据集 |
| `detection/build_yolo_dataset.py` | 窗口 → YOLO 格式。`--train-source` / `--val-clean-frac` / `--debias-*` / `--min-window-frac` |
| `detection/train_yolo.py` | 两段式训练。`--rect` / `--patience` |
| `detection/evaluate.py` | **双口径评测**:AP@0.30/0.50/0.75 + 检出率/时间偏差/虚警率 |
| `detection/inspect_predictions.py` | 预测框叠加真值,人工判断"虚警是不是真 burst" |
| `detection/render_windows.py` | 窗口+框渲染,含框心位置泄漏检查 |

---

# 通宵批次结果(2026-08-14)与接下来的行动方案

9 组全部 `rc=0`,总计 394 分钟。方案与三处改动见 `OVERNIGHT_PLAN.md` 文末"实际执行"。
评测口径统一为 `evaluate.py --imgsz 640 --conf 0.05 --class-agnostic`(未传 `--rect`,九组一致)。
基线 `v5_single` 用**同一份 val、同一条命令**重测,**精确复现 0.361**,所以下面的对比协议是对齐的。

## 结论一(唯一确定的):噪声底 σ = 0.056,是原先假设的 5.1 倍

同配置只换种子:

| seed | AP@0.50 |
|---|---|
| 0(`o_clean_neg`) | 0.327 |
| 1 | **0.242** |
| 2 | 0.355 |
| 3 | 0.366 |

**均值 0.323,σ = 0.056,极差 0.124。** 此前全文一直在用的 σ 是 **0.011**。

**必须一起说的三个限制:**
1. 旧 σ 是在**配置 C(243 训练框)**上测的,今晚是**当前配置(570 框、clean-only、yolo7)**,
   两者不是同一个量,**这不构成对旧数字的直接推翻**。
2. n=4 估的 σ 自身 95% 区间是 **[0.032, 0.209]**,仍然极松 —— 今晚这个 σ 同样别太当真。
3. σ 主要由 seed1(0.242)这一个离群点撑起来,另外三个种子是 0.327/0.355/0.366,相当紧。
   n=4 时一个坏 run 就能主导 σ。

### 连带影响:"B vs C 是最扎实的结论"需要降级

那条结论的 +0.085 当时按合计噪声 0.03 算是 2.8 倍;**按 σ=0.056 只有 1.5σ**。
同样受限于上面第 1 条(不同配置),但**它已经不能再被称作"最扎实的结论"**。
本文档前面"标注质量 > 标注数量"的方向性判断仍有另外两条独立佐证支撑
(AP50-95 首次跃升、Type II 从 0.001 到 0.114),但**那个具体的数值差距不再显著**。

## 结论二:没有任何一组赢过基线,污染剔除无可测效果

| 组 | AP@0.50 | AP@0.30 | AP@0.75 | 检出率 | \|Δt\| 中位 | vs 0.361 | σ 倍数 |
|---|---|---|---|---|---|---|---|
| **v5_single(基线)** | **0.361** | 0.459 | 0.177 | 88% | 7.8s | — | — |
| `o_clean_neg` | 0.327 | 0.461 | 0.153 | 88% | 6.4s | −0.034 | 0.6σ |
| `o_nofreeze` | 0.347 | 0.464 | 0.137 | **96%** | 6.1s | −0.014 | 0.2σ |
| `o_ep60` | 0.285 | 0.388 | 0.127 | 90% | 6.0s | −0.076 | 1.4σ |
| `o_wide` | **0.130** | 0.220 | 0.046 | 78% | 8.0s | −0.231 | **4.1σ** |

**A 组(剔除 74 个污染训练负样本):测不出效果(0.6σ)。** 四个种子的均值 0.323 比基线低
1.4 个标准误,方向上偏负但不显著。检出率、时间偏差与基线持平。
**这不等于"剔除是错的"** —— 是"在 σ=0.056 这把尺子下,这个量级的改动看不见"。

## 结论三:学习率搜索**根本没有发生**(我的失误)

`o_clean_neg` / `o_lr_hi` / `o_lr_lo` 返回**逐位相同**的全部指标。根因:
**ultralytics 的 `optimizer=auto` 会覆盖用户传入的 `lr0`**,九组实际全部跑在
`AdamW(lr=0.002)` 上,传进去的 3e-4 / 3e-5 只被记进 `args.yaml`,从未生效。

**烟测为什么没抓到**:我验证的是"`lr0` 出现在 `args.yaml` 里",那只证明**参数被记录**,
不证明**它被使用**。真正的证据一直在日志里(`optimizer: AdamW(lr=0.002...)`),没去看。
**教训与本文档已有的那条同源:验证要验到"真的生效",不是"看起来传进去了"。**
代价:约 74 分钟白跑。

意外收获:三组指标完全一致,**反证了管线在固定种子下完全确定性**。

## 结论四:`o_wide` 的 −0.231 不能读成"时间分辨率有害"

它是唯一超过 2σ 的结果,但相对 `o_clean_neg` **同时改了四个变量**:
图宽 640→1280、`imgsz` 640→1280、`batch` **16→8**、`rect` **False→True**;
而且 21 epoch 就早停(其余 30-36),训练期最佳 mAP50 仅 0.081。
**能说的只是"这个配置整体差得多",不是"时间分辨率翻倍有害"。** 要干净回答原问题,
必须只改渲染尺寸,保持 batch 和 rect 不变。

附带一个反常:`rect` 在这里只造成 0.129 vs 0.130 的差异,而本文档前面记录它曾造成 0.027。

## 总体判断:模型侧调参到此为止

在 570 训练框这个规模上,**σ 已经和我们试图检测的所有效应同量级**,模型侧的改动测不出来。
这与本文档"五个瓶颈"的诊断一致 —— **瓶颈在标注和 val 规模,不在模型**。
优先级回到扩充干净标注。

---

## 接下来几小时的行动方案(2026-08-14 下午,无人值守)

按"能不能在无人确认下安全完成"排序。每项完成后在本节回填结果。

### 1. 修 `optimizer=auto` 覆盖学习率 —— 让 `--lr0-*` 真的可用
不加 `--optimizer` 的话,今后任何人重跑 C 组还会掉进同一个坑。加 `--optimizer`
参数,并在启动时**打印实际生效的学习率**,让"没生效"这件事在日志里一眼可见。

### 2. 把 maxconf 写进 `evaluate.py`(本文档 P1)
实测 F1 0.311 → **0.522**,是能部署的口径。现在所有数字报的都是原始口径,
两者差距很大。做成 `--maxconf` 开关,默认关闭(保持历史数字可比),开启后在
AP 和运营指标两套口径上同时生效。完成后用它重报基线模型。

### 3. 写"模型辅助找目录漏记事件"脚本并跑一遍 —— **本轮最高价值**
目录漏记约 36%,而模型在 conf≥0.1 上判断"这里有 burst"的准确率是 24/24。
做法:用最好的模型(`v5_single`)扫**全部 6532 个窗口**,列出"高置信度但目录没有"的
检出,渲染成审核图 + CSV,人醒了只做确认。这是当前补干净标注最快的路径,
而且**同时扩 train 和 val**,直接对准上面"σ 太大"和"标注天花板"两个瓶颈。

**已知的偏差要写进产出里**:模型在自己训练过的 train 负样本上被教过"这里没有 burst",
开火概率被系统性压低(train 1.9% vs val 4.2%),所以在 train 窗口上会**漏挖**,
挖出来的数量是下限,不是全部。

### 4. 不做的事,以及为什么
- **不刷新 `boxes.csv`**:约 90 条审核仍未进 boxes。但现在刷新没有立即收益(近期不重训),
  却会让 `yolo7_*` 与 `boxes.csv` 脱节,多制造一个复现边界。
  **正确时机是下次建数据集之前,一次性吸收"这 90 条 + 第 3 项确认出来的新事件"。**
- **不再调超参**:见"总体判断"。
- **不重跑干净版 wide 实验**:值得做,但要占 1.5 小时 GPU 且结论不紧急,留给醒后决定。

### 执行结果(2026-08-14 下午,三项全部完成)

**1. `optimizer=auto` 已修** —— `train_yolo.py` 新增 `--optimizer`(默认 `auto`),并在
`auto` + 自定义 lr 同时出现时**在启动横幅里打印警告**。已双向验证:
`--optimizer AdamW --lr0-stage1 7e-3 --lr0-stage2 5e-4` 时 ultralytics 日志报
`AdamW(lr=0.007)` / `AdamW(lr=0.0005)`,**与传入值一致**;默认参数下警告不误报。
**这次验证的是 ultralytics 自己打印的生效值,不是 `args.yaml` 里的记录值** —— 上次就栽在这个区别上。

**2. maxconf 已进 `evaluate.py`**(`--maxconf`,默认关闭以保持历史数字可比)。
同时新增**单一工作点的 P/R/F1 表** —— 没有它,maxconf 的作用在输出里根本看不见,
因为 AP 会在置信度扫描上积分,而 maxconf 是在**固定工作点**上拿召回换精确率。

`v5_single` 两套口径(同一份 val):

| 口径 | 框数 | AP@0.30 | AP@0.50 | AP@0.75 | 召回 | 精确 | **F1** | 虚警/h |
|---|---|---|---|---|---|---|---|---|
| 原始 | 818 | 0.459 | 0.361 | **0.177** | 72% | 20% | 0.311 | 1.43 |
| **--maxconf** | 268 | **0.550** | **0.388** | 0.160 | 57% | **48%** | **0.520** | **0.51** |

原始口径**逐位复现**了此前记录的 818 框 / 0.361 / F1 0.311,说明改动没有污染老路径。
F1 0.520 与文档记载的 0.522 一致(框数 268 vs 232 是聚类实现的细微差别)。
**注意 maxconf 让 AP@0.75 和 |Δt| 中位变差**(0.177→0.160,7.8s→12.0s):
它保留的是簇里置信度最高的框,不是位置最准的框。

**3. 目录漏记挖掘已完成** —— `detection/mine_catalog_gaps.py`(新增),
扫描 5898 个窗口(已排除少数派配置和过短窗口),**产出 160 个"模型认为有 burst、目录没有"的候选**:

| 置信度 | 数量 |
|---|---|
| 0.10-0.20 | 109 |
| 0.20-0.35 | 39 |
| 0.35-0.50 | 11 |
| >0.50 | 1 |

- 按划分:unused 110 / val 33 / **train 17** —— **train 这一栏被系统性压低,别当真实差异读**
  (模型在自己的 train 负样本上被训练过"这里没有 burst")
- **102 个候选落在"目录里完全没有事件"的窗口上** —— 这些正是以空标注参与训练的反向监督样本
- 产出:`detection/gap_review/gap_candidates.csv`(全部 160 条,含 `verdict` 空列)
  + `gap_sheet_01..15.png`(置信度最高的 120 个,全窗 + ±150s 放大双栏)

**肉眼抽查最高置信度的 8 个:6-7 个框内有清楚的 burst 结构**,与"conf≥0.2 时准确率接近 100%"吻合;
`#4` 是明确的失败(框落在 burst 左侧的空白处)。**多个窗口里目录记了较弱的那个 burst、
却漏掉了同窗更强的那个** —— 漏记不是"只漏弱信号"。

**下一步需要人**:填 `gap_candidates.csv` 的 `verdict` 列(y/n/?),然后才谈得上写回 `boxes.csv`。
**没有任何候选被自动写进训练数据**,这是刻意的 —— 低置信度段的确认率只有 80%。

**过程中踩到的坑(已记,别再犯)**:等待脚本用 `pgrep -f "mine_catalog_gaps.py"` 轮询,
**匹配到了等待脚本自己的 bash 命令行**,挖掘早已跑完却空转了 20 分钟。
这正是 `OVERNIGHT_PLAN.md` 里白纸黑字警告过、且在 `run_overnight.sh` 里刻意规避掉的同一个坑,
换个脚本又踩了一次。**结论:这台机器上不要用 `pgrep -f <脚本名>` 做等待条件。**

### 代码修正与 Phase 2 兼容性工作(2026-08-14 傍晚)

抓取 ASSA 之前必须先做的三件事,全部已验证。

**1. `keep_dominant_config` 改成带容差的分组** —— 不改这个,抓来的数据会被静默丢掉

原实现是**精确元组匹配**,两个后果:
- ASSA 自己的两个子配置 `14.995-86.933`(1536)和 `15.000-86.938`(1299)相差 **5 kHz**,
  本来就是同一套设置的两种写法,却被当成两个配置,**只保留大的那个,悄悄丢掉 1299 个窗口**
- 更糟:两站合并时 Arecibo(`15.000-86.625`)和 ASSA(`14.995-86.933`)是不同的键,
  它会**只保留窗口多的那一站、把另一站整个丢掉** —— 跨站实验的反面,而且不会报错

改为:通道数和采样间隔**必须精确相同**(它们直接决定像素网格,200 vs 400 通道不是舍入差异),
频率上下界按 **1% 相对容差**贪心聚类。1% 足够合并舍入差异(86.625 vs 86.933 差 0.36%),
又能把真正不同的仪器分开(Arecibo 的 5-119.9MHz 在 freq_min 上差 200%;ASSA 的 400ch/108-524MHz 更远)。

四个单元测试全过:Arecibo 单站仍是 5908、ASSA 两子配置合并成 2835、两站合并后
**8743 且两站都在**、400ch 与 200ch 仍然分开。

**2. 新增 `--stations` —— 这不是锦上添花,是让上面那个容差修改变安全的前提**

容差合并的直接副作用:**Arecibo 和 ASSA 现在属于同一个配置组**。
所以一旦 ASSA 数据进入同一个 `windows.csv`,重建"Arecibo 单站 Phase 1 数据集"会**静默吸收 ASSA 窗口**。
`--stations` 让每个数据集明说自己由哪些站构成;未指定且池子里有多站时会打印警告。
站名写错直接报错退出,不猜。

**3. ASSA 抓到独立目录 `data/ecallisto/windows_assa/`,不进 `data/ecallisto/windows/`**

即使有了 `--stations`,把 ASSA 追加进共享的 `windows.csv` 也会让**本文档和
`OVERNIGHT_PLAN.md` 里记录的所有历史命令**行为发生变化 —— 那些命令是要被重跑的。
独立目录让 Phase 1 保持逐字节可复现;Phase 2 合并时再显式拼 CSV + 软链 npy,是个刻意动作。

**回归验证(最关键的一条)**:改完之后用
`--stations Arecibo-Observatory` 重建 yolo7_single,与今天全部结果所用的那份
**images 和 labels 逐字节相同**(`diff -r`)。`mine_catalog_gaps.py` 选出的窗口数也仍是 5898。
**今天所有已发布的数字不受这次改动影响。**

**4. `apply_gap_verdicts.py`(新增)——把确认结果落地,但只落地安全的那一半**

确认一个候选值两件事,风险完全不同,脚本刻意拆开:
- **删除错误监督(做)**:目录无任何事件的窗口现在是空标注训练图,在教"这里没有 burst"。
  加进 `contaminated_negatives.csv` 即可,**纯减法,不发明任何坐标**。
- **新增正样本框(不做)**:把模型自己画的框写回 `boxes.csv`,等于拿模型输出训模型,
  而**框的范围恰恰是它最弱的一维**(AP@0.75 0.16;maxconf 保留的是最自信的框,不是位置最准的)。
  这是在已知最差的维度上自我蒸馏。确认的候选只输出**待标注清单**,由人在 `event_review` 里拖框定时间。

安全性照抄 `merge_subset_reviews.py` 的约定:**默认 dry-run**、写前时间戳备份、只增不改。
已测:dry-run 不落盘、写入后旧行全保留/无重复/列不变、**重复执行幂等**、非法 verdict 直接拒绝。

**由此发现的一件事:挖掘的增量价值比预想的小**

160 个候选里有 **102 个落在"目录完全没有事件"的窗口**上,但它们只涉及 **90 个不同窗口,
而这 90 个已经全部在 `contaminated_negatives.csv` 里** —— 0 个新增。
不是 bug:那份名单本来就是同一个模型、同一个 conf≥0.10 阈值扫负样本得到的,挖掘只是把它重新发现了一遍。

**真正的新信息是另外 58 个候选**(涉及 57 个窗口),它们落在**目录已经记了别的事件**的窗口上
——即"这个窗口目录记了一个 burst,但漏了另一个"。之前那次污染排查只看负样本窗口,够不到这些。
其中 conf≥0.2 的有 22 个。**所以人工确认的收益主要在这 58 个身上,不在那 102 个。**

**5. Australia-ASSA 抓取已启动**(2026-08-14 20:30,pid 15484,日志 `detection/scrape_assa.log`)

冒烟测 1 个 station-day 通过后启动全量:736 个 (station,date),实测 ~42s/天,
**预计 ~8.6 小时**(文档旧估计 17.1h 偏保守),约 2.7 GB,磁盘余 67 GB。
输出的 windows.csv / boxes.csv 列结构与 Arecibo 那份完全一致。

**一个抓取前查出来的事**:冒烟测抓到的第一天(2021-01-19)是 **400ch/108-524MHz 微波段**,
不是我们要的 200ch/15-87MHz。查了一下,**568 个 station-day 里只有 27 个(4.8%)是纯 400ch**,
浪费可接受,不值得为它加日期过滤。但这说明 ASSA 会在两个差别极大的波段之间切换,
`keep_dominant_config` 之后会把 400ch 那部分丢掉,**别把"抓了 736 天"当成"多了 736 天可用数据"**。

**ASSA 的框全部是未经人工修正的 catalog 时间。** 按本文档已确立的结论
(往训练集混未审核数据在当前量级是负收益),**不要直接把 ASSA 并进训练集就重训** ——
它的用途是让 Type II/V 第一次有足够样本(两站合并 II 107、V 58),而这需要先审。

**审核图补齐(同日修正)**:第一次只渲染了 `--sheets-top 120`,而候选有 160 个 ——
**40 个候选没有图**,全部落在 conf 0.100-0.119 最低档(22 个 conf≥0.2 的优先候选都有图,
所以高价值部分没漏)。已补齐,并新增 `--from-csv` / `--subset` 渲染路径:
**从已有 CSV 重渲染、不重跑推理、不改写 CSV**(所以已经手填的 verdict 不会被覆盖)。
`--subset new-info` 直接渲染那 58 个"目录记了别的事件却漏了这个"的候选。

当前 `detection/gap_review/`:
- **`PRIORITY_01..08.png`** —— 58 个新信息候选,**先审这批**
- `gap_sheet_01..20.png` —— 全部 160 个
- `gap_candidates.csv` —— 全部 160 条,填 `verdict` 列(y/n/?)

## 漏记框已写回(2026-08-15)

126 条经人工确认 + 标注的漏记 burst 已落地。用户决定**全部 126 条采用**(含 49 条保留模型框的)。

### 写在独立文件里,不是 boxes.csv —— 否则会被静默抹掉

`refresh_boxes.py` **不是追加,是从 Monstein 目录整个重建 `boxes.csv`**。漏记框按定义不在目录里,
所以写进 `boxes.csv` 的话,**下次刷新会把这 126 条全部删除** —— 而"刷新以吸收积压的 90 条审核"
本来就在路线图上。因此它们存在 `data/ecallisto/windows/gap_boxes.csv`,
由 `build_yolo_dataset.py --gap-boxes` 在构建时合并,目录重建碰不到它。

### `time_source` 记录每一行的来源

| | 条数 | 含义 |
|---|---|---|
| `gap_manual` | 77 | 审核者自己拖的框 |
| `gap_model` | 49 | 原样保留了模型的框(位比特级相同) |

`event_review` 只在范围偏离预填值时才写 `manual_burst_range_json`(判据 `<1e-6`),所以这两类可以
精确区分。保留率随置信度**单调降到 0**(0.10-0.15 档 66% → ≥0.35 档 0%),支持"高置信度框得准所以没动"
这个解读。

**但两类的框宽差异很大,这个数字要记住**:

```
gap_manual   中位 74s   (四分位 41-167s)
gap_model    中位 19s   (四分位 16-25s)   <- 窄 4 倍
现有 manual  中位 40s
```

`gap_model` 那批集中在 16-25s 的窄区间,不像在跟随真实 burst 时长。**没有据此排除它们**(用户已确认
逐条看过),但 `time_source` 让"结果异常时能不能怪这 49 条"从猜测变成可查。

### 顺带回收了 63 个被整窗丢弃的窗口

`contaminated_negatives.csv` 从 **90 条降到 27 条**:其中 63 个窗口现在有了真实标注,
不再是"含隐藏 burst 的负样本",而是普通正样本窗口,重新进入训练。
**从"扔掉 90 个窗口"变成"扔掉 27 个、回收 63 个带标注的正样本"。**

### 构建结果与两个必须知道的后果

```
train  570 -> 648 框 (560 -> 618 个有框窗口)
val    224 -> 243 框 (581 -> 557 张图)
```

1. **126 个框里有 29 个没进数据集**,全部因为**同窗还有未审核/弃用的 catalog 框**。
   这是正确行为,不是丢失:那种窗口如果只写 gap 框,被弃用的那个真实 burst 就成了"图里有 burst 但没框",
   正是反向监督。整窗排除是对的。
2. **⚠️ val 集变了,`0.361` 这个基线不再直接可比。** 新 val 是旧 val 的**真子集**(少 24 张,
   没有新增),原因是 `choose_val_days` 按每类框预算挑天,多了 126 个可用框之后选中了不同组合,
   `20220625` 一整天被换出去。属于预期行为。
   **下一轮训练必须在新 val 上重测基线,不能拿 0.361 当对照。**
   而且 σ=0.056 意味着新基线本身也要跑 3 个种子。

### 类型

125 条标 III,1 条标 II(`gap0067`,20230509 18:53:05-18:59:59,约 7 分钟,长度符合 II)。
类型由审核者指定。**注意 Type II 的已知坑**:`clean()` 会在背景拟合阶段吃掉长 Type II 约 97% 的
超出量,这条框本身没问题,但以后对它做去噪要小心。

## 过夜批次 2(2026-08-15 03:03 启动,与 ASSA 抓取并行)

`detection/run_overnight2.sh`,日志 `detection/overnight2.log`。数据集 `yolo8_single`
(648 train / 243 val 框,含 126 个漏记框)。

**为什么只跑 4 个种子 + 1 个分辨率臂,不堆实验**:σ=0.056 之下,小于 ~0.11 的差异读不出来,
多加对照臂只会产出无法解读的数字。今晚的任务是**在变化后的 val 上重建基线**(gap 框改变了 val 划分,
0.361 不再可比)并**重测当前数据规模下的 σ**。分辨率臂是因为上一次 o_wide 同时改了四个变量而作废。

**并行安全性(实测,不是估计)**:GPU 抓取完全不用(它是网络+FITS 解码),T4 空闲;抓取常驻内存仅
0.38 GB;唯一真实竞争是 **CPU 只有 4 核**,而 ultralytics 默认 8 个 dataloader worker 会挤掉抓取的
解码步骤 —— 已给 `train_yolo.py` 加 `--workers`,今晚用 2。**停掉了 streamlit(占 4 GB)**,标注已完成。
实测:训练启动后抓取速率 113 → 120 s/天,慢约 6%,可接受。
