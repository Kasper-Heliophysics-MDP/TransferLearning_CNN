# eCallisto → 本站 迁移学习训练方案

写给下一个要执行这个计划的人(或下一次会话)看的。哪些地方是"查过真实数据"、哪些地方是"推理但没
验证"、哪些地方"只有跑代码才能知道",全文会标清楚,不要混为一谈。

> **2026-08-11 更新**:这份文档最初写成时"转换代码、训练代码一行都没写",现在**不是了**。已经落地的:
> `ecallisto_grabber/extract_windows.py` + `scrape_windows.py`(15 分钟窗口抽取,11 天试点验证过)、
> `detection/build_yolo_dataset.py`(YOLO 格式转换,坐标 round-trip 验证过)、`detection/train_yolo.py`
> (两段式训练,smoke test 通过)、`detection/render_windows.py` / `render_boxes.py`(渲染 + 泄漏检查)。
> **文档中段"分阶段计划"里的部分数据来源描述已被后面的"画框渲染验证结果"推翻过一轮,以靠后的章节为准。**

> **2026-08-12:Phase 1 已实际跑完四组,结果和瓶颈诊断在 `PHASE1_RESULTS.md`,以那份为准。**
> 本文档里有三处已被实测推翻:①"给框加频率范围能放宽 IoU"(收益恒为零,频率项在二维 IoU 里约掉);
> ②"一维区间 IoU 比二维更严苛"(反了,我们的满高度口径其实更宽松);③ 留白第 7 项猜测的"时间轴
> 压缩是 AP50-95 的瓶颈"(真因是标注天花板 —— IoU≥0.95 命中率恒为 0.0%)。细节见新文档"本轮的
> 三处更正"。

## 目标

先在 eCallisto 数据上验证"COCO预训练YOLO + 固定窗口目标检测"这套 pipeline 本身可行(能跑通、loss
降、预测框合理),再考虑往本站数据迁移——这是回答"eCallisto预训练到底有没有用"的实际路径。两件事
明确分成两个阶段,不要在还没验证机制本身之前就去解决更难的跨域对齐问题。

## 架构选择:YOLO(COCO预训练)+ 固定窗口目标检测

### 为什么不是"逐事件裁剪成定长图再分类"

原来的思路是把每个burst事件裁剪成适配它自己时长的图,再做分类。这个思路有个结构性问题:burst时长
差异极大(Type III可能几秒,Type II可能几分钟),要把长短不一的事件都压缩进同一个像素宽度,必然要用
不同的时间压缩比,而这会让"同样的真实漂移速率"在不同事件里表现成不同的像素斜率——这正是分类要学的
关键特征,被这个操作污染了。

### 改用的方案:目标检测 + 固定尺寸画布

不裁剪去适配每个事件,而是用**固定时长的画布**(不管这段时间里有没有burst、burst多长),在画布里用
目标检测框出burst的位置和类型。短burst框窄、长burst框宽,画布本身的像素比例不变,不需要为了适配单个
事件的时长而拉伸压缩。

### 文献依据

查证过、不是凭空想的。2023-2025年多篇论文做的正是"e-Callisto数据 + 深度学习 + Type II/III(/IV/V)
检测"这件事,做法高度收敛:

- **e-Callisto网站本身的标准输出就是固定15分钟窗口的频谱图**,多篇论文直接拿这个当输入单位,不是
  按每个已知burst单独裁剪([Multi-category solar radio burst detection based on task-aligned
  one-stage object detection model](https://arxiv.org/pdf/2503.16483))
- 用**目标检测**(YOLOv5/v7/v8、Deformable DETR、task-aligned one-stage detector)而不是整图分类
- 数据规模参考(task-aligned one-stage detector论文):6126张图/7584个burst实例,Type II 338、
  Type III 5736、Type III-s 1296、**Type IV 只有47、Type V只有167**,这么小的量在检测范式下依然
  做到 mAP@50: II 0.860、III 0.803、III-s 0.814、IV 0.706、**V 0.810**——比按纯分类思路预想的
  "V要大几百才够"乐观不少
- **YOLO做transfer learning是标准做法,不是特例**:YOLO官方发布的权重本身就是COCO(33万图、80类)
  预训练的,查到的论文里YOLOv8就是和VGGnet-19/ResNet-152/DenseNet-201等分类骨干放在一起比较的
  transfer learning方法之一,F1在87-92%区间([Type II and Type III Solar Radio Burst
  Classification Using Transfer Learning](https://arxiv.org/html/2512.11487))

### 没解决、文献里也没解决的部分

查过的这几篇论文**都是多站点数据直接混合训练,完全没有处理跨站点频段不一致的问题**,方法部分只字
未提。更广的射电天文机器学习文献(LOFAR相关)明确把"varying observation durations and frequency
ranges"列为公认难点,但也没给出成熟方案。这不是我们想多了,是这个领域目前也没人认真解决的地方。

## Bounding box 构造

- **时间范围**:用 `event_review` 人工修正后的时间(不是catalog原始标注——真实审核发现催录标注的
  时间在几乎每个站点都大量出现偏差,通常偏早,细节见 `event_review/HANDOFF.md`)
- **频率范围**:用频谱图满高度,不精确标注burst实际扫过的频率范围。原因:框的坐标不编码频率信息了,
  但框内的像素内容还在,分类该学的视觉形状信息没有丢失,只是不要求模型输出精确的频率定位——如果后面
  发现分类效果被这个简化拖累,这是一个可以回头补的优化点,现在没有证据支持需要现在就解决。
  **已在真实图上渲染验证过,满高度合理**,见下面"画框渲染验证结果"
- **零宽度 catalog 标注的默认框**:36% 的正样本 `event_start_time == event_end_time`,没有宽度信息。
  已定的默认值是 **`catalog_start + 10s`,宽 `35s`**。这不是拍脑袋定的,是在 69 个"零宽度且有人工修正"
  的事件上网格搜出来的:

  | 默认框 | 中位 IoU | IoU≥0.5 命中率 |
  |---|---|---|
  | **catalog +10s,宽 30-40s** | **0.50** | **62%** |
  | +20s,宽 20s | 0.40 | 46% |
  | +20s,宽 55s | 0.27 | 9% |
  | 居中 55s(最初的直觉猜测) | 0.12 | 1% |

  做过 200 次 split-half 验证(一半拟合、一半打分),留出集 IoU≥0.5 = **58%**(5-95 分位 46-69%),
  跟样本内 62% 接近,是轻微乐观而非过拟合。**两个前提**:这 69 条是"被人挑出来修正过"的样本、不是随机
  样本,可能偏向明显偏移的那些;而且只在 Arecibo 上拟合过,换站点要重新算

## 数据现状(真实数字,不是估计)

### 抓取规模

eCallisto:8站抓取,人工审核证实 ROSWELL-NM 数据质量差(14/14真实审核全部discard)已完全移除;
GREENLAND人工审核证实是这批里信号质量最好的站点,已扩大抓取到1029条。最终7站(Australia-ASSA、
Arecibo-Observatory、SWISS-Landschlacht、EGYPT-Alexandria、SWISS-MUHEN、SPAIN-PERALEJOS、
GREENLAND)共约7330条事件。类型只有 III/II/V/0(负样本)四种——**Type IV/I不在数据里**,
`scrape.py` 当初 `--types II III V` 已经在抓取源头过滤掉了,不需要额外处理。

### 审核发现的真实问题(不是靠数值指标或读代码发现的)

- **own-station的known_burst_weight bug**:`extract_events_own_station.py` 全文件去噪那一步
  没有把catalog真实burst时间传进去做保护,靠人工审核第一批6条真实审核5条报告"burst被白框抹去"才
  抓出来,已修复并重新生成全部74条own-station数据
- **ROSWELL-NM数据质量差**:数值指标(新旧算法都测过)都没拦住,人工审核14/14全部discard
- **catalog标注时间系统性不准**:跨几乎所有eCallisto站点大量出现,不是个别站点的问题——这是
  `event_review` 新增"人工时间段修正"功能的直接原因

### 有效样本量估算

第一轮198条真实审核(7站,ROSWELL已排除):discard 40.4%、usable 35.4%、case_by_case 24.2%
(其中91.7%只是时间标注不准,理论上可用人工时间修正功能修复)。**有效可用率约57%**,套回4300+原始
正样本,实际有效数量约**2400-2500**,不是4300+。这个比例本身有局限(198条集中在几个站点,不能完全
当全局比例用),但比"数据已经够了"的说法靠谱。

## 关键决策:频段对齐只在"迁移到本站"这一步才需要,Phase 1不需要

### 频段对齐的技术方案(已查证,留到后面用)

太阳射电暴学界有标准做法:**插值到公共频率网格**。查到的具体实现
([e-CALLISTO FITS Analyzer](https://arxiv.org/html/2603.26086)):公共网格用参与源里最小的通道
间隔构造(`Δf_ref = min_k(Δf_k)`),各源用最近邻bin映射到这个网格上——**前提是频段有重叠或相邻,
完全不重叠的频段这个方法也没解决**。

我们的7个eCallisto站点跟本站(16-24MHz)重叠情况:**全部7站都有7-8MHz重叠**(本站总共8MHz带宽),
这不是巧合,是当初筛选候选站点时就把"频段跟本站有重叠"设成了硬指标。所以这个技术在我们的场景下是
适用的——但只在真正要做本站迁移的阶段才需要用。

### 关键的真实测量:burst信号实际落在16-24MHz的比例远低于预期

用真实审核确认过"usable"的eCallisto事件抽样21条(排除1条当天配置本身不含16-24MHz的),**肉眼逐条
判断**(自动化的能量比值指标试过,肉眼验证后发现不可靠——分不清是真实burst还是持续性RFI残留,虚高
报告了94%这种不可信的数字,已作废):

| 判断 | 数量 |
|---|---|
| 清楚可见 | 5/20 |
| 勉强/存疑 | 4/20 |
| 看不出来/分不清是不是RFI | 11/20 |

**估算范围约25-35%**,不是精确统计,是比自动指标可信得多的粗略范围。套到2400-2500个有效正样本,
真正能在16-24MHz窗口里用于训练的可能只有**大约600-900个**——如果"对齐到本站频段"是唯一路径,这个
数字偏紧。

**额外发现**:SWISS-Landschlacht 和 SWISS-MUHEN 两个站点,抽到的6个样本在16-24MHz范围内**全部零
命中**,而且反复出现一种贯穿整个观测窗口的规律性周期斜纹图案(不集中在burst时间附近)——不像真实
太阳暴,更像这两个站点在这个频段有持续性周期干扰源。这两个站点用于"对齐到本站"这个特定目的时价值
存疑,但不影响它们在Phase 1(不需要对齐)的价值。

### 决定:Phase 1 不做频段对齐,不受16-24MHz限制

Phase 1的目标只是验证pipeline机制,不涉及本站,不需要对齐到16-24MHz,上面这个"25-35%"的限制**在
Phase 1完全不适用**——直接用各配置的原生频段/分辨率即可,数据量回到2400-2500这个级别(而不是
600-900)。频段对齐、16-24MHz可见比例这些,是Phase 3(本站迁移)才需要重新面对的问题。

## 数据质量策略:训练集容忍噪声,验证集必须干净

深度学习对训练数据里一部分噪声/错标通常有容忍度(不是特例,是分类任务里很成熟的经验)。但这次是
**检测任务**,噪声体现在框的位置可能不对,不是单纯标签类别错——框错位置和标签错类别,模型的容忍
机制不完全是一回事,检测任务对定位噪声的容忍度有没有同等程度的文献支撑,**没有查过,这是一个已知
的、比"分类噪声容忍"更弱的类比,需要在实际训练结果里观察验证**。

策略:
- **训练集**:直接用未审核数据,不用等审完。第一轮结果如果精度不理想,再回头看是不是噪声比例导致的,
  到时候有的放矢地扩大审核范围
- **验证/测试集必须是审过的干净数据**——标签本身如果是错的,测出来的F1没法解读

## 分阶段计划

> **注意:下面 Phase 1/2 的数据来源已经被"画框渲染验证结果"那一节推翻过一轮**(现有 crop 有框居中
> 泄漏,不能直接用)。数据来源改成重抓的 15 分钟窗口,但每个 Phase 的**目的、类别选择、流程**不变。

### Phase 1(当前优先级):Arecibo单一配置,eCallisto-only pipeline验证

**数据来源(已修订)**:重抓 Arecibo 的 15 分钟窗口(9 小时 / 6.6 GB,见上文"决定:改用按 FITS 边界
对齐的 15 分钟窗口")。筛选条件必须包含 `freq_min_mhz == 15.0`(见结论4)。
~~直接用现有 `raw_events/` 的 2551 条 crop~~ —— 框心 100% 居中,mAP 不可解读。

**审核结果照常复用**:已审核的 269 条 Arecibo 记录里 263 条(97.8%)落在 15-86MHz 这个配置里,
**144 条 usable + 99 条 discard** 依然是干净的验证/测试集种子;人工时间修正(132 条)按事件身份
(location + date + event 时间 + type)映射到新窗口上,不因为换了切法而失效。

**目标类别**:优先 III vs 负样本,II 顺带做(54个原始量级,文献里类似规模也出过能用的结果)。
**V不强求在这一步出现**(这个配置里原始只有31个)——Phase 1的目的是验证机制,不是一次性训好四个类。
**负样本不再单独采样**:15 分钟窗口里没有任何目录事件的就是负样本(空标注),`sample_negative_windows()`
那套逻辑在新流程里用不到了。用户已确认负样本不做人工审核直接用。

**含非 II/III/V 事件的窗口要整窗排除**:既不能给框(不是我们的类别),也不能算干净负样本(里面真的有
burst)。判定必须用**全类型**的目录(`explode_by_station` 未过滤版),不是只用 II/III/V 的那份。

**流程**:COCO预训练YOLOv8,先冻结backbone训练检测头几轮,再解冻整体微调。环境已确认就绪(T4 GPU、
PyTorch+CUDA、ultralytics库都已安装)。**图像用 raw**(逐行减中位数即可清楚看到 burst,已在渲染验证里
确认),不用 `cleaned_events/`,原因见结论3。

**产出**:一个真实的F1/mAP数字,证明或证伪这套pipeline机制。

### Phase 2:跨站点混合对照实验

同样流程,换成全部7站原生数据混合(不做任何频段对齐),对比F1相比Phase 1单一配置子集掉多少——这个
对比能给"物理刻度不统一到底伤害有多大"这个问题一个真实数字,不需要额外开发成本,只是换数据源重跑。
Australia-ASSA(同配置2839条,当前是第二大候选)可以作为这一阶段扩大数据量的来源。

**去噪 A/B 实验的做法已修订**:原计划是"`raw_events/` vs `cleaned_events/` 同 pipeline 各跑一遍",
**现在不能这么跑** —— 结论3 证实 `cleaned_events/` 对被人工修正过的事件是坏的(42% 的真实 burst 区域
在生产清洗时完全没有保护,肉眼确认 burst 被抹掉)。拿它当对照组,跑出来只能证明"用错时间去噪会毁掉
数据",回答不了"去噪有没有用"。

可行的替代:**只在"有人工修正时间、并用修正时间重新清洗过"的子集上做 A/B**。这个子集重洗很便宜
(单文件 ~0.05s)。未审核的大多数事件修不了 —— 它们的 catalog 时间同样错,重洗也是拿错时间去保护,
所以**整批重洗没有意义**。

**如果要去噪,顺序仍然是先在各自原生分辨率去噪,再重采样对齐,不要反过来**——重采样会引入相邻像素间
的人为相关性,去噪算法的统计假设(基于真实独立采样点)在插值后的数据上没有验证过。

### Phase 3:频段对齐 + 本站迁移验证

只有确认Phase 1/2机制可行之后才启动。需要:
1. 用"公共频率网格"技术把eCallisto数据对齐到本站16-24MHz(技术方案已查证,见上文)
2. 面对"burst实际可见比例只有25-35%"这个限制,决定是接受有效数据量缩水到600-900,还是找别的路径
   (比如只用确认可见的子集,或者放宽对齐目标频段)
3. 本站数据**不进入训练集**,只用来验证"eCallisto训出来的检测器,在本站数据上表现如何"——这是
   真正回答"这套transfer learning有没有用"的地方
4. 本站有完整连续CSV,做真实的固定窗口切片(不受"只有事件buffer裁剪"的限制)比eCallisto现有数据更
   方便,值得为本站单独设计窗口构造流程,而不是复用eCallisto那套

## 环境(已确认)

- GPU:Tesla T4,15GB显存,基本空闲
- PyTorch 2.12.0+cu130,CUDA可用
- ultralytics 8.4.80(YOLOv8官方库)已安装

## 画框渲染验证结果(已完成 —— 原留白第2项的答案)

用 `detection/render_boxes.py` 在真实 Arecibo 频谱图上把框画出来肉眼看过了(cleaned 和 raw 各一轮,
同样12个事件),配合纯算术统计。**"满频率高度"这个简化没问题,但"用哪个时间"这件事比预想的严重得多。**

### 结论1:满频率高度画框 —— 合理,可以照做
raw 图上 burst 基本都纵向贯穿 15-86MHz 大部分高度,满高度框没有明显浪费。唯一杂质是最底部若干通道
有一条恒定的饱和条带(Arecibo 频率轴末尾有 8 个通道是重复的 padding 值),每张图都有、位置固定,
对检测器是常量背景,可以先不管,真要处理就是裁掉最底部几行。

### 结论2:catalog 时间不能直接拿来当框 —— 两个独立的硬伤
在 Arecibo 15-86MHz 子集 1694 个正样本上统计:
- **36% 的 catalog 标注是零宽度**(`event_start_time == event_end_time`),YOLO 里是退化框,不能用。
  这不是"框不准",是"根本没有宽度信息"。
- 在 132 个有人工修正的事件上,**80% 的修正是往后挪,66% 挪动超过 10 秒**,中位数 +20s、最大 +230s。
  这就是审核阶段早已发现的"catalog 标注系统性偏早",这里是它的定量版本。
- 非零宽度的 catalog 框中位宽度(60s)其实跟人工修正后(55s)接近,但 **38% 的框宽度超过真值的 2 倍**
  (见过一个 Type II 标了 1310s 而真实信号只占其中一段)。所以宽度是"大部分还行、尾部很糟",
  跟时间偏移那个"系统性、普遍"的问题不是一个量级。

### 结论3(意料之外,但影响最大):`cleaned_events/` 对被修正过的事件是坏的
production 的 `cleaned_events/` 是用 **catalog 时间**建 `known_burst_weight` 跑出来的,而人工修正说明
catalog 时间是错的 —— 所以保护窗口保护的是空白处,真实 burst 在没有 ground truth 保护的情况下被去噪。
纯算术统计(修正后的框 vs 生产清洗实际保护的 `catalog±10列` 区间),132 个有修正的事件里:

| 修正框被保护的比例 | 事件数 |
|---|---|
| 0%(完全没保护) | 56 / 132(42%) |
| < 50% | 82 / 132(62%) |
| 100%(没问题) | 23 / 132(17%) |

肉眼验证完全吻合:同样12个事件的 raw 图上 burst 清清楚楚(有几个还很亮),cleaned 图上同一位置一片
空白 —— **burst 被抹掉了**。这跟 own-station 那个 `known_burst_weight` bug 是同一类问题,但成因不同:
代码没错,是 `cleaned_events/` 生成在人工时间修正之前,数据过期了。

推论:
- 之前"审核时看到 burst 偏淡/被抹除"的判断,有一部分可能是这个原因造成的假象,不是去噪算法本身的问题。
- **Phase 2 计划里的 raw vs cleaned A/B 实验不能直接跑** —— 现在的 `cleaned_events/` 对这批事件是被
  污染的对照组,跑出来只能证明"用错时间去噪会毁掉数据",不能回答"去噪有没有用"。要么先用修正后的
  时间重新清洗,要么这一轮直接用 raw。
- Phase 1 用 raw 起步是更省事也更安全的选择:raw 图只需要逐行减中位数就能清楚看到 burst。

### 结论4:Phase 1 的"单一配置"不是真的单一
`Arecibo + 0.25s + 200通道` 这 2551 条里,**2530 条是 15-86.625MHz,另外 21 条是 5-119.9MHz**。
筛选条件要再加一个 `freq_min_mhz == 15.0`,否则这 21 条会以完全不同的物理刻度混进来。

### 结论5(最严重):现有 crop 存在标签泄漏,**不能直接用来做检测训练**
测了正样本框在 crop 里的归一化水平位置:

```
normalized box CENTER within crop:  mean 0.5000   std 0.0000   min 0.500   max 0.500
落在 0.49-0.51 区间: 100%   (n=1694)
```

**1694 个正样本,框心无一例外正好在图正中央,标准差是 0。** 这是构造导致的必然结果 ——
`extract_events.py` 的 crop 定义就是"事件 ± `buffer_s`(60s)",事件永远居中。

对检测任务这是致命的:YOLO 只要恒定在水平正中央画一个框就能拿到很高的定位分。Phase 1 的**唯一目的**
是"拿一个真实的 mAP 数字证明或证伪这套 pipeline",但在这份数据上跑出来的数字无法解读 —— 分不清模型
学会的是"识别 burst"还是"框画中间"。**这不是精度问题,是实验设计失效。**

同一个问题也波及"从现有 crop 拼固定时长画布"这条捷径:除非在窗口位置上做随机抖动,否则框还是居中;
而 buffer 只有 60s,抖动余量很小、还随事件长度变化(20s 的事件 crop 只有 140s,连 180s 画布都凑不出来)。

## 决定:改用按 FITS 边界对齐的 15 分钟窗口(需要重抓)

结论5 直接否掉了"复用现有 crop"的所有变体。改成文献的标准输入单位:**一个 15 分钟窗口 = 一张图**,
窗口内的每个目录事件 = 一个框。这一步同时解决三件事:画布时长统一、框位置泄漏消失、负样本变成自然产物
(窗口内没有任何目录事件 = 空标注),不再需要 `sample_negative_windows()` 那套单独的负样本采样逻辑。

### 为什么必须重新下载
原始 FITS **没有缓存**(确认过,磁盘上只有无关的旧 scratchpad 文件)。`extract_events.py` 当初是边下边裁、
只存 crop。所以要拿到 15 分钟窗口只能重新下载。

好消息是 `fetch.fetch_station_day()` **本来就是整天下载**(列出当天该站全部 FITS、全下、拼成全天数组),
所以重抓的网络量跟 phase 1 完全一样,不是额外开销,只是换个切法存下来。

### 成本(天数取 metadata 实际 station-day,files/day 和耗时取 scrape 日志实测中位数)

| 站点 | 天数 | 事件 | 全天缓存 | **±30分钟缓存** | 仅精确15分钟 | 下载耗时 |
|---|---|---|---|---|---|---|
| Arecibo-Observatory | 432 | 2567 | 15.2 GB | **6.6 GB** | 1.8 GB | 9.0 h |
| Australia-ASSA | 568 | 2942 | 19.2 GB | 7.9 GB | 2.0 GB | 17.1 h |
| GREENLAND | 160 | 775 | 7.6 GB | 2.1 GB | 0.5 GB | 4.4 h |
| EGYPT-Alexandria | 65 | 277 | 3.1 GB | 0.8 GB | 0.2 GB | 1.8 h |
| SWISS-Landschlacht | 65 | 284 | 2.8 GB | 0.8 GB | 0.2 GB | 1.7 h |
| SWISS-MUHEN | 65 | 249 | 2.2 GB | 0.7 GB | 0.2 GB | 1.3 h |
| SPAIN-PERALEJOS | 65 | 236 | 2.1 GB | 0.7 GB | 0.2 GB | 1.3 h |
| **7 站合计** | | | **52.2 GB** | **19.8 GB** | **5.0 GB** | **36.5 h** |

**注意"全天缓存"比早期口头估的 29 GB(Arecibo)小得多**——那个数字错在按 24 小时观测算,实测
Arecibo 中位数只有 49 个文件/天(约 12 小时,站点只在本地白天观测),真实是 15.2 GB。

### 已定的缓存策略:±30 分钟(事件所在 15 分钟槽 ± 2 槽)
- 全 7 站总量 **19.8 GB**,比"只缓存 Arecibo + Australia 全天"(34 GB)还少,磁盘余量 85 GB 毫无压力
- 覆盖到 **75 分钟画布**(5 个槽)。文献标准是 15 分钟,没人提过要超过 1 小时,所以以后改画布长度不会
  再触发重新下载
- 对比"仅精确 15 分钟"的 5.0 GB:多花约 15 GB 买掉"以后改窗口长度就要重下"的风险,这个保险很便宜
- 全天缓存多出的 32 GB 换到的只是"支持任意长度画布",大概率永远用不到
- **缓存粒度不影响下载时间** —— 无论如何都是整天下载,36.5 小时(全 7 站)是固定的。所以"先只做
  Arecibo"省的是那 9 小时,不是省磁盘

### 存储格式
按 FITS 边界对齐的 15 分钟 **uint8 `.npy`,一个槽一个文件**。任何画布长度都是本地拼接,不再碰网络。

### 执行顺序
1. **先只做 Arecibo**:9 小时下载,6.6 GB
2. 以后要加站点,每站成本已知且有界(最贵的 Australia-ASSA 是 17 小时 / 7.9 GB),7 站全上限 20 GB
3. 磁盘如果紧张:根目录 `burst_data.zip`(17.6 GB)是 `data/burst_data/` 的压缩备份,用户已确认**以后
   可以删除**

## 明确留白 / 只有实际跑代码才能回答的问题

这些不是"还没讨论清楚",是"讨论到头了,继续讨论不会有新答案,只有写代码跑一次才知道":

1. **15 分钟窗口 → YOLO 格式标注的转换代码**——正在写(`ecallisto_grabber/extract_windows.py` +
   `scrape_windows.py`)。原来那条"`review_status.csv` → YOLO"的表述已过时:数据单元从事件 crop 变成
   15 分钟窗口之后,`review_status.csv` 的角色从"数据来源"降为"框修正 + 质量筛选的补充输入"
2. ~~用人工修正后的时间+满高度画框,叠加在真实图上到底合不合理~~ **已渲染验证,见上文**
3. 检测任务对"框位置噪声"的真实容忍度——类比自分类任务的标签噪声容忍度,但检测任务的噪声性质不同,
   没有查到对等的文献支撑,需要从Phase 1的真实训练结果里观察。**渲染验证之后这一项的风险比原来估计
   的高**:未审核事件用的就是 catalog 时间,36% 零宽度(套 +10s/35s 默认值,留出集 IoU≥0.5 只有 58%)
   + 系统性偏早。不是"框稍微不准",是"三分之一是统计猜出来的、其余大半位置偏了"
4. ~~负样本(type=0)在YOLO格式里应该是空标注的图,转换逻辑还没设计~~ **改用 15 分钟窗口后自然解决**:
   窗口内无任何目录事件 = 空标注文件。剩下的实现细节只有"含非 II/III/V 事件的窗口整窗排除"
5. Phase 1/2/3各自的真实训练/评估耗时——没有估过
6. ~~零宽度 catalog 框要怎么处理~~ **已定:`catalog_start + 10s`,宽 `35s`**,依据见"Bounding box 构造"
7. **15 分钟窗口在 YOLO 里的图像尺寸/长宽比**——一个 200×3600 的窗口喂给 `imgsz=640` 会被压成什么样、
   要不要按频率轴上采样让长宽比不那么极端,没有验证过。所有窗口尺寸一致意味着缩放比例一致(泄漏和
   斜率失真都消失了),但"极端长宽比对 YOLO 的检测精度有多大影响"是另一个只能跑出来才知道的问题
8. **目录漏收 burst 会压低 precision,程度未知**——11 天试点的渲染图上能看到目录没收录但肉眼明显的
   burst(活跃日的 Monstein 目录不会逐条收录每个 Type III,Type VI "一串 Type III" 这个类别的存在
   本身就是证据)。检测器正确检出未标注 burst 会被算成误报。**解读 Phase 1 的 mAP 时必须记得这一层,
   不能把低 precision 直接当成模型不行**;真要量化,只能靠人工审一批预测框看有多少"误报"其实是对的

## 试点验证已完成(11 个 Arecibo station-day,2022-01)

137 个窗口 / 95 MB / 平均 64s 每 station-day,跟"432 天 / 9 小时 / 6.6 GB"的预估一致。

**核心目的达成——标签泄漏消失**:

```
框心归一化位置:  mean 0.4680   std 0.2912   min 0.011   max 0.966
落在 0.49-0.51:  6%              <- 每事件 crop 时代是 100%,std = 0.0000
```

试点过程中在真实数据上抓出并修掉三个 bug(越界排除规则被 CTM 长条目放大成全天团灭、目录笔误撞上
跨午夜处理变成 24 小时框、跨窗口边界的 1 秒碎片框),全部细节见 `ecallisto_grabber/开发日志.md`
"模块三"。**三个都是"代码逻辑没问题、撞上目录脏数据"的类型,读代码看不出来。**

## YOLO 转换 + 训练脚本已就绪(smoke test 通过)

- `detection/build_yolo_dataset.py` —— `windows.csv` + `boxes.csv` + `win-*.npy` → ultralytics 格式
  (`images/{train,val}` + `labels/{train,val}` + `data.yaml`)
- `detection/train_yolo.py` —— COCO 预训练 YOLOv8,两段式(先冻结 backbone 训检测头,再整体微调)

### 三个不是默认值的决定(写在脚本 docstring 里,改之前先读)

1. **划分按 station-day,绝不按窗口**。同一天相邻的 15 分钟窗口共享 RFI 环境,而且一个事件可能骑在两个
   窗口上(试点里有 3 个这种框)。按窗口划分会把近似重复的上下文放到 train/val 两侧,虚高验证分数。
2. **验证集只收人工审核过的干净窗口**。一个 val 窗口只有在"是负样本(构造上就干净)"或"它的每个框都被
   审为 usable"时才保留;val 日里不满足的窗口**直接丢弃,不移到 train**——移过去会让同一天出现在两侧,
   把第 1 条破坏掉。
3. **时间轴会被压缩**。窗口是 200×3600(1:18),默认 640×640 下时间轴压约 5.6 倍,中位 35s 的框约 25px 宽。
   这个压缩对每个窗口**完全一致**(正是 15 分钟窗口改造要的性质),但压这么多会不会损精度是留白第 7 项,
   `--img-size 640 1280` 是最直接的下一个实验。

**几何增广全部关掉**(`fliplr/flipud/mosaic/degrees/shear/perspective/scale/translate` 全为 0):
频谱图不是照片,竖直翻转会反转频率漂移方向(Type II/III 最关键的判据),水平翻转反转时间,mosaic 会把
不相关的窗口沿时间轴拼接。这些在 COCO 上有用的增广在这里是直接破坏标签。

### 坐标转换已做 round-trip 验证
把导出的 label 文件反解回绝对列号,跟 `boxes.csv` 逐框比对:**38/38 框配平,最大列误差 0**。
(cx/w 写错这种 bug 不会报错,只会让训练悄悄不收敛,必须显式验证。)

### smoke test 结果(11 天试点数据,81 训练图 / 14 验证图,3 epoch)
两段式流程跑通,GPU 正常,val 集加载正确(`nt_per_class=[0,14,0]`,与导出的 14 个 Type III 框一致),
metrics 正常计算,权重正常保存。**数字本身没有意义**(3 epoch、24 个训练框),recall 0.214→0.571、
mAP50 0.0018→0.0074 只说明"在学",不说明任何精度结论。这一步验证的是管线有效性,不是模型效果。

## 下一步

1. Arecibo 全量重抓进行中(432 station-day,~9 小时,6.6 GB → `data/ecallisto/windows/`)
2. 抓完跑 `build_yolo_dataset.py`,**先看数据集统计**(尤其 val 有几天、多少干净正样本)再决定训练配置
3. Phase 1 正式训练,产出一个可解读的 mAP —— 解读时记得留白第 8 项(目录漏收 burst 会压低 precision)

## Phase 1 第一次训练结果(已完成)—— 数字很差,但原因已定位

`yolov8s` COCO 预训练,10 epoch 冻结 backbone + 60 epoch 整体微调,imgsz 640。

| 指标 | 值 |
|---|---|
| 最佳 mAP50 | **0.162,出现在第 3 个 epoch**,之后 57 个 epoch 再没超过 |
| 最终 mAP50 / Type III | 0.147 / **0.102** |
| 文献同类工作 Type III | ~0.80 |

**不要据此判断架构不行。** 下面三层证据说明数字被一个可测量、可修复的问题主导。

### 1. 过拟合(损失曲线)
train box_loss 1.041→0.354、cls_loss 2.164→0.335;同期 val box_loss 3.198→**3.593**、
cls_loss 4.533→**5.393**。1596 个训练框跑 60 epoch 严重过量,最佳点在第 3 个 epoch。

### 2. 模型找得到位置,定不准范围(看预测框)
val 图上 19 个预测里 **18 个与真值框重叠**——不是随机乱报。但置信度**最高只有 0.44**,负样本上最高
0.178(有区分度但从不自信),而且同一处常有 2-3 个嵌套框,是"不确定范围"的典型症状。
mAP50-95(0.01-0.08)远低于 mAP50,同样指向定位而非检出。

### 3. 根因:train 和 val 的标注来源根本不是一套约定

| | 框数 | time_source | 审核状态 |
|---|---|---|---|
| **val** | 149 | **manual 121(81%)** + catalog 28 | 100% usable |
| **train** | 1596 | catalog 1095 + zero_width_default 501,**manual 0** | 100% 未审核 |

val 标注是干净的(人工修正过),train 标注**全部是未修正的 catalog 约定**。而 catalog 约定跟人工真值的
一致性实测只有:

| train 的 catalog 框(非零宽度) | IoU≥0.5 命中率 |
|---|---|
| **当前用法(不做任何修正)** | **27%** |
| 起点 +25s、宽度上限 90s | **63%**(留出集 55%,5-95 分位 41-66%) |

**模型忠实学会了 catalog 约定(系统性偏早),然后被拿去跟修正后的约定打分。** 预测图上能直接看到这个
偏差:多个窗口里预测框整体压在真实 burst 的**左侧**,正是"catalog 偏早"被学进去的样子。

所以 mAP50 0.16 主要衡量的是**两套标注约定的分歧程度**,不是模型的检测能力。

### 下一步(按性价比排序)
1. **给 train 的 catalog 框做去偏**(起点 +25s、宽度上限 90s):影响 69% 的训练框,标注质量翻倍
   (27%→55% 留出集)。**风险要说清楚**:这 63 个样本是"人工选择去修正的",是有偏样本;其中 21% 的
   catalog 框本来就是对的,全局平移会让这部分变差。属于用可测的系统偏差换整体质量,不是无损操作。
2. **大幅削减 epoch 数 + 早停**(最佳点在第 3 个 epoch,当前跑 60 个纯属有害)。
3. `--img-size 640 1280` 减轻时间轴 5.6× 压缩(留白第 7 项)。
4. 扩大人工审核量——最有效但最贵,也是唯一能同时改善 train 和 val 的办法。

### 仍未回答
低 precision 里有多少是"检出了目录漏收的真实 burst"(留白第 8 项)。这一轮预测框绝大多数落在真值附近,
所以**这个因素在当前结果里不是主要矛盾**,但去偏重训之后需要重新看。

## 去偏对照实验结果(第二轮训练)

改动:train 的 `catalog` 框整体 +25s、宽度上限 90s(1109 个框);freeze 5 + 25 epoch,`patience 8` 早停。
**val 与第一轮逐字节相同**(已 `cmp` 验证),1064 个 train 标注文件改变 —— 单变量对照。
早停在第 14 epoch 触发(最佳第 6),训练量从 70 epoch 降到 19。

两个 `best.pt` 在同一 val 集上重新评测(不是拿"最后一个 epoch"混比):

| | all mAP50 | all 50-95 | **III AP50** | III 50-95 | III P | III R |
|---|---|---|---|---|---|---|
| baseline(60ep,不去偏) | 0.146 | 0.081 | **0.099** | 0.040 | 0.256 | 0.163 |
| debias(+25s/cap90,早停) | 0.054 | 0.015 | **0.147** | 0.040 | 0.199 | **0.326** |

### 结论1:Type III 确实变好了 —— AP50 +49%,recall 翻倍
Type III 有 141 个 val 框,是唯一统计上说得过去的类。去偏后模型**找到了两倍的 burst**。
预测图上也能看到:第一轮里几个漏掉或压偏的窗口,这一轮框移到了 burst 上,最高置信度从 0.44 升到 ~0.63。

### 结论2:**聚合 mAP50 在这个数据集上不可用,必须单独看 Type III**
聚合值从 0.146 掉到 0.054,方向和 Type III 相反。原因是 II 只有 **3** 个 val 框、V 只有 **5** 个:
baseline 在 II 上撞到 AP50=0.337,debias 是 0.003 —— 3 个框上的运气差异,却主导了三类平均。
**以后报告 Phase 1 结果一律以 Type III 为准,聚合 mAP 只能当噪声看。**

### 结论3:定位精度没有改善,代价是 precision
III 的 AP50-95 两轮都是 0.040,完全没动。也就是说去偏让模型**更能找到 burst(IoU 0.5 这个宽松阈值下)**,
但**框的贴合度没有变好**;同时预测数量增加,precision 从 0.256 掉到 0.199。这和"去偏后标注一致性只到
55%(留出集)"是自洽的 —— 系统偏差修掉了,剩下的随机误差还在。

### 需要更正一处先前的判断
第一轮我说"预测框整体压在真实 burst 左侧",那是看几张图得出的印象。**聚合测量不支持这么强的说法**:
预测中心相对最近真值中心的中位偏移,baseline 是 -0.0059、debias 是 -0.0023(归一化单位,900s 窗口下
约 -5.3s → -2.1s)。方向对得上、去偏也确实把它减半了,但**量级远没有"整体压偏"那么大**,当时的措辞
过强了。

### 下一步
1. `--img-size 640 1280` 减轻时间轴 5.6× 压缩(留白第 7 项)——AP50-95 卡住不动,时间分辨率是最可疑的
   下一个瓶颈
2. 扩大人工审核:val 只有 149 个框(II 3 个、V 5 个)使得除 III 外的任何结论都不可读,train 的标注
   一致性也只有 55%。这是唯一能同时解决两头的办法
3. 不建议继续在去偏参数上调优 —— 55% 已经接近这 63 个有偏拟合样本能支撑的上限

## 参考文献

- [Type II and Type III Solar Radio Burst Classification Using Transfer Learning](https://arxiv.org/html/2512.11487)
- [Multi-category solar radio burst detection based on task-aligned one-stage object detection model](https://arxiv.org/pdf/2503.16483)
- [Automated detection and classification of solar radio bursts in CALLISTO spectrograms using deep-learning YOLOv5 model and ensemble methods](https://www.swsc-journal.org/articles/swsc/full_html/2026/01/swsc250104/swsc250104.html)
- [Solar Radio Burst Detection Based on Deformable DETR](https://arxiv.org/html/2503.18287)
- [e-CALLISTO FITS Analyzer: A Software Framework For CALLISTO Solar Radio Data](https://arxiv.org/html/2603.26086)
- [The ROAD to discovery: Machine-learning-driven anomaly detection in radio astronomy spectrograms](https://www.aanda.org/articles/aa/full_html/2023/12/aa47182-23/aa47182-23.html)

## 相关文档

- 根目录 `HANDOFF.md`——整个项目的交接文档,own-station/eCallisto数据处理进展
- `event_review/HANDOFF.md`——人工审核工具的代码结构、使用说明、开发进度
- `detection/render_boxes.py`——把 catalog 框 / 人工修正框 / 生产清洗实际保护区间三者叠在真实频谱图
  上的渲染脚本,上面"画框渲染验证结果"就是用它跑出来的。用法:
  `python detection/render_boxes.py <输出目录> --pick usable_corrected --n 12 [--raw]`
